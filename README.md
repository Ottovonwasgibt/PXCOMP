# PXCOMP

PXCOMP is an experimental desktop image compositor built around an exact pixel-ownership rule: for a project with `N` source photographs, every output pixel belongs to exactly one source, with no overlap and no holes, and each source receives an equal target share of the canvas.

Development happens on feature branches. See the active build PR for the current desktop application.
